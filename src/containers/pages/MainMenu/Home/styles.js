import {} from 'utils';

const styles = {
  background: {
    position: 'absolute',
    top: 0,
    bottom: 0,
    left: 0,
    right: 0,
  },
  container: {
    height: '100%',
    width: '100%',
  },
  imageBackground: {
    resizeMode: 'cover',
    height: '100%',
    width: '100%',
  },
};

export default styles;
