import {METRICS, FONTS} from 'utils';

const styles = {
  imageBackground: {
    resizeMode: 'contain',
    height: '100%',
    width: '100%',
  },
  backButton: {
    marginLeft: METRICS.gutter.s,
    marginVertical: METRICS.gutter.xxl,
  },
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
};

export default styles;
